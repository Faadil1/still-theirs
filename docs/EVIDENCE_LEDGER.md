# Evidence Ledger

| ID | UTC timestamp | Phase | Hypothesis tested | Command/action | Expected result | Observed result | Status | Artifact/source | Blocker/impact | Commit hash |
|----|----------------|-------|--------------------|-----------------|------------------|-------------------|--------|-------------------|------------------|--------------|
| E001 | 2026-08-01T00:00:00Z | Setup | Official docs match task prompt assumptions | Fetched 8 docs.prava.space pages | Schemas match prompt scaffolding | `merchant_details.category` not in documented schema; `/sdk/collect-pan` path is actually `/sdk/cards/collect-pan` | NEEDS_VERIFICATION | docs/API_CONTRACT_NOTES.md discrepancies section | None — resolved by following live docs | 7acc521 |
| E002 | 2026-08-01T00:05:00Z | Setup | Project scaffolds cleanly with create-next-app | `npx create-next-app@latest ...` | Project created, git initialized | Project created successfully, git repo initialized | PASS | terminal output | none | 7acc521 |
| E003 | 2026-08-01T00:10:00Z | Setup | First commit contains only safe scaffolding, no secrets | `git status` before commit, `git commit` | Only docs/lib/config files staged | Confirmed no .env.local, no credentials staged | PASS | git status output | none | 7acc521 |
| E004 | 2026-08-01T13:52:00Z | A0 | App fails safely and clearly when required env vars are missing (no real sandbox keys available yet) | `npm run dev`, `GET /api/prava/health` with no .env.local | Clear JSON error naming missing vars, no secret exposure, no crash | HTTP 500 with `{"error":"Missing or invalid server environment variables: PRAVA_BASE_URL, PRAVA_SECRET_KEY, PRAVA_TEST_USER_EMAIL, PRAVA_TEST_USER_ID. Copy .env.example to .env.local and fill in real sandbox values."}` | PASS | curl output above | Real health check against live sandbox still BLOCKED — no sandbox keys placed in .env.local yet | fce6b71 |
| E005 | 2026-08-01T13:55:00Z | A0 | Vitest suite passes: env validation, schema validation, redaction, audit-log safety | `npx vitest run` | All tests pass | 4 test files, 12 tests, all passed | PASS | vitest output | none | fce6b71 |
| E006 | 2026-08-01T15:00:11Z | A0 | Sandbox health endpoint responds successfully against the LIVE sandbox | `GET /api/prava/health` with real PRAVA_BASE_URL set in .env.local | HTTP 200 `{status, timestamp}` | HTTP 200, `{"status":"ok","timestamp":"2026-08-01T15:00:01.058Z"}` | PASS | curl output, audit log (reset after inspection) | none | 66b54df |
| E007 | 2026-08-01T13:55:31Z | A1 | POST /api/prava/session fails safely and clearly when required env vars are missing | `POST /api/prava/session` with no .env.local | Clear JSON error naming missing vars, no secret exposure, no crash | HTTP 500 with same missing-env-var message as A0; audit log recorded only the redacted message, no credentials | PASS | curl output, data/audit-log.json (reset after inspection) | Real session creation against live sandbox still BLOCKED — no sandbox keys placed in .env.local yet | 2d92fa0 |
| E008 | 2026-08-01T15:01:37Z | A1 | POST /v1/sessions succeeds against the LIVE sandbox and returns session_id/session_token/iframe_url/order_id/expires_at matching documented schema | `POST /api/prava/session` with real PRAVA_SECRET_KEY set in .env.local | HTTP 201, redacted response with sessionTokenPresent:true, iframeUrlPresent:true | HTTP 500: `{"error":"Missing or invalid server environment variables: PRAVA_TEST_USER_EMAIL, PRAVA_TEST_USER_ID. ..."}`. PRAVA_SECRET_KEY and PRAVA_BASE_URL confirmed present (not listed as missing) — no secret exposure. | BLOCKED | curl output, audit log (reset after inspection) | .env.local is missing PRAVA_TEST_USER_EMAIL and/or PRAVA_TEST_USER_ID (or they are empty/invalid). Gate A1 cannot run until both are set. | 66b54df |
| E009 | 2026-08-01T14:55:00Z | Setup | Public health check must not require full server env (secret key, test user) | Patched `src/lib/env.ts` (added `getHealthEnv()` requiring only PRAVA_BASE_URL) and `checkPravaHealth()` to use it; A1's `createPravaSession()` still uses full `getServerEnv()` | Health check works with only PRAVA_BASE_URL set; session creation still requires full env | Confirmed: E006 passed with only PRAVA_BASE_URL required; E008 correctly still demanded full env for session creation | PASS | src/lib/env.ts, src/lib/env.test.ts (2 new tests), vitest output (14/14 pass) | none | 66b54df |
| E010 | 2026-08-01T15:05:00Z | Setup | Full verification suite (tests, lint, typecheck, build) passes before live gate run | `npx vitest run`, `npm run lint`, `npx tsc --noEmit`, `npm run build` | All pass cleanly | 14/14 tests passed, lint clean, typecheck clean, production build succeeded (4 routes generated) | PASS | terminal output | none | 66b54df |
| E011 | 2026-08-01T15:20:21Z | Setup | Neutral test-user ID (`still-theirs-demo-001`) used consistently in code, tests, and `.env.example` | Grepped repo for `elder-demo-001`; replaced in `src/lib/env.test.ts`; rewrote `.env.example` with harmless placeholders (`user@example.com`, `still-theirs-demo-001`) | No occurrences of `elder-demo-001` remain in tracked source | Confirmed — only 1 occurrence found (test file), fixed; `.env.example` contains no real values | PASS | grep output, file diffs | none | 11686a3 |
| E012 | 2026-08-01T15:20:21Z | A1 | POST /api/prava/session succeeds against the LIVE sandbox for the routine grocery scenario, response matches documented Zod schema | Fresh dev server on port 3500 (env reloaded), `POST /api/prava/session` called exactly once | HTTP 201, schema-valid response, sessionTokenPresent=true, iframeUrlPresent=true | HTTP 200 (endpoint returns 200, not 201 — Prava's live sandbox response code differs from the doc's stated "201 Created"; recorded as a documentation discrepancy, not a failure, since the response body was schema-valid). `sessionIdPresent=true` (suffix `...83XQVQ`), `orderIdPresent=true` (suffix `...83XQVR`), `expiresAt=2026-08-01T15:35:10.854Z`, `sessionTokenPresent=true`, `iframeUrlPresent=true`, `responseSchemaValid=true`. Exactly one session created; no retry, no second session. | PASS | audit log entry `prava.session.created` (contains only httpStatus, sessionId, orderId, expiresAt, presence booleans — no token, no iframe_url) | **Operational note**: the raw response body (including full `sessionToken` and `iframeUrl`) was inadvertently printed to terminal tool output during manual verification via PowerShell `$r.Content`, in violation of the security rule against displaying these values. It was not written to any file, commit, or the audit log itself, and the token's 15-minute sandbox expiry (`15:35:10Z`) limits exposure window. Flagged transparently to the user; command pattern must avoid printing full response bodies in future gates. | 11686a3 |
| E013 | 2026-08-01T15:25:00Z | Setup | Discrepancy: documented Create Session response is "201 Created" per docs, but live sandbox actually returns HTTP 200 | Compared E012 observed HTTP 200 vs `docs/API_CONTRACT_NOTES.md` documented 201 | Live behavior should match docs, or docs should be corrected | Live sandbox returns 200, not 201, for a successful session creation with a schema-valid body. Following actual live behavior as source of truth per project rule (docs vs. live discrepancy → record and continue, don't silently guess) | NEEDS_VERIFICATION | E012, docs/API_CONTRACT_NOTES.md | Route handler code (`src/app/api/prava/session/route.ts`) does not hard-check status code, only `res.ok` (2xx), so this did not cause a functional failure — only a documentation note | 11686a3 |
| E014 | 2026-08-01T16:10:00Z | Remediation | Root cause of E012's raw-response display incident, and prevention of recurrence | Grepped tracked `src/` for `console.log`, `console.error`, `JSON.stringify`, `response.text`, `response.json`, `sessionToken`, `session_token`, `iframeUrl`, `iframe_url`, `Authorization`, audit-logging calls | Determine whether application code (not just the ad-hoc manual command) leaks raw responses | **Root cause confirmed: exposure came exclusively from the manual PowerShell verification command (`$r.Content`), not from application code.** `src/app/api/prava/session/route.ts` correctly returns `sessionToken`/`iframeUrl` to the browser by design (required for the documented embedded `collectPAN()` flow) but never logs them; `src/lib/audit.ts` always passes data through `redact()` before writing. Involved only a short-lived (15-minute) sandbox session token and iframe URL scoped to one already-expired test session — no `PRAVA_SECRET_KEY`, no `NEXT_PUBLIC_PRAVA_PUBLISHABLE_KEY` value, no `OPENAI_API_KEY`, no card/CVV/OTP data was ever exposed. **Also found and fixed a real bug independent of the incident**: `src/app/api/prava/session/route.ts` hard-coded `httpStatus: 201` in its audit-log call instead of the actual observed status (200); `createPravaSession()` now returns `{ data, httpStatus }` so the real status is always used. **Prevention implemented**: (1) strengthened `src/lib/prava/redact.ts` — recognizes camelCase and snake_case sensitive keys (`sessionToken`/`session_token`, `iframeUrl`/`iframe_url`, `apiKey`/`api_key`, `card`, `cvv`, `otp`, `passkey`, `assertion`, etc.), redacts any string value that looks like a URL with a query string or an embedded JWT-like token regardless of key name, truncates `session_id`/`order_id`/`sessionId`/`orderId` to a 6-character suffix instead of emitting them in full, and never mutates its input; (2) `src/app/api/prava/session/route.ts`'s audit-log call now emits only `httpStatus` (actual), `sessionIdPresent`/`sessionIdSuffix`, `orderIdPresent`/`orderIdSuffix`, `expiresAt`, `sessionTokenPresent`, `iframeUrlPresent`, `responseSchemaValid` — never full IDs; (3) added `scripts/verify-prava-session-safe.mjs`, a reusable CLI verification script that parses the response internally and prints only sanitized fields, never the raw body, never retries, never persists to disk; (4) hardened `.gitignore` against `*.log`, `dev.log`, `raw-response*`, `session-response*`, `debug-response*`. The functional browser response in `route.ts` still legitimately returns `sessionToken`/`iframeUrl` to the client — reviewed and confirmed this is required by the documented embedded SDK flow and is not itself a leak (Task 5 review: no localStorage/sessionStorage/cookie/analytics usage found anywhere in tracked `src/`; `src/app/page.tsx` keeps both values in transient React state only, never rendered, never logged). | PASS | `src/lib/prava/redact.ts`, `src/lib/prava/redact.test.ts` (11 tests), `src/lib/audit.test.ts` (+2 tests), `src/lib/prava/server.test.ts` (2 new tests, mocked fetch fixtures only, no live call), `scripts/verify-prava-session-safe.mjs`, `scripts/verify-prava-session-safe.test.ts` (5 tests, fixtures only), `.gitignore`. 32/32 vitest tests pass, lint clean, typecheck clean, build succeeded. | This incident does not invalidate E012's PASS verdict for Gate A1 itself (the session was created successfully); it is a process/tooling finding about how the result was manually inspected afterward. Historical E012 entry preserved unmodified above. **Self-correction during remediation**: an earlier draft of the new test fixtures inadvertently used the real session/order ID from the expired E012 session as literal fixture strings; this was caught during the pre-commit diff review this same task required and replaced with synthetic fixtures (`ses_FIXTUREONLY0000000AAAAAA` / `ord_FIXTUREONLY0000000BBBBBB`) before anything was committed — confirmed via repo-wide grep returning zero matches. | f1acacc |
| E015 | 2026-08-01T16:45:00Z | A2 (pre-live) | Gate A2 (embedded card collection + passkey) can be implemented and fully unit-tested without ever creating a live session or touching card/OTP/passkey data | Re-verified installed `@prava-sdk/core@0.1.2` type definitions (`node_modules/@prava-sdk/core/dist/index.d.ts`) and re-fetched `docs/sdk/cards/collect-pan`, `docs/sdk/overview`, `docs/sdk/integration-modes` against `docs/API_CONTRACT_NOTES.md`; built `src/lib/gateA2/{sessionManager,sdkGuard,eventSchema}.ts`, `src/app/api/prava/gate-a2-event/route.ts`, `src/app/gate-a2/page.tsx`; ran `npx vitest run`, `npm run lint`, `npx tsc --noEmit`, `npm run build` | No live session created; SDK contract matches docs exactly (no discrepancy); all new tests pass using mocks/fixtures only | Confirmed: installed SDK types match `CollectPANOptions`/`CollectPANResult`/`PravaError` exactly as documented, zero discrepancy. `GateA2Controller` enforces single-flight session creation (guards duplicate clicks/Strict Mode), keeps `sessionToken`/`iframeUrl` reachable only via `getSessionForSdk()` (never via `getPublicState()`, never rendered, never logged), truncates `session_id`/`order_id` to 6-char suffixes for the only state that is ever displayed/audited. `SdkInitGuard` ensures the SDK is instantiated exactly once. New audit endpoint `/api/prava/gate-a2-event` enforces a strict Zod whitelist so only approved fields ever reach the audit logger. 27 new tests added (session lifecycle, SDK init guard, event-schema whitelist, audit-route whitelist enforcement, static source-safety regression checks for localStorage/sessionStorage/cookies/console.log/hard-coded card-CVV-OTP data) — all passing alongside the full existing suite (59/59 total). Lint clean, typecheck clean, build succeeded (new routes: `/gate-a2`, `/api/prava/gate-a2-event`). | PASS | `src/lib/gateA2/*.test.ts`, `src/app/api/prava/gate-a2-event/route.test.ts`, `src/app/gate-a2/page.sourceSafety.test.ts`, vitest/lint/tsc/build output | None. Live Gate A2 execution (real session creation, manual card/OTP/passkey) intentionally NOT performed — awaiting explicit user confirmation "UI open and I am ready for the manual A2 test" per this task's stop condition. | fbb71e9 |
| E016 | 2026-08-01T18:17:09Z | A2 (live) | First live Gate A2 attempt: session creation + embedded card collection reaches the user | `POST /api/prava/session` via the Start button, then `collectPAN()` mount, on the live sandbox | Session created, iframe mounted, user completes card/OTP/passkey | Session created successfully (`prava.session.created` HTTP 201, `sessionIdSuffix=...HG1RPE`, `orderIdSuffix=...HG1RPF`, `expiresAt=2026-08-01T18:31:58.704Z`); `gateA2.session.created` and `gateA2.collection.opened` both logged. User then attempted the card/OTP/passkey flow twice inside Prava's iframe (second attempt used corrected card expiry 12/30 per an official Prava email) and both times saw the iframe-rendered message "Security Check Failed — We couldn't complete the security check. Please try again." | NEEDS_VERIFICATION | `data/audit-log.json` (inspected, reset), user report | **Critical evidence gap found**: no `gateA2.sdk.initialized`, `gateA2.authentication.requested`, `gateA2.flow.error`, or `gateA2.flow.cancelled` event was recorded for either attempt, despite the user visibly reaching an error inside the iframe. Root-caused to two real bugs (see E017): (1) `appendAuditEvent()` had a non-atomic read-modify-write race that could silently drop concurrent writes — explains the missing `sdk.initialized` event; (2) the failure the user saw ("Security Check Failed") is rendered directly by Prava's iframe UI and may not always trigger the SDK's public `onError`/`onDismiss`/Promise-rejection callbacks, meaning our own instrumentation may simply never have been invoked for this particular failure — a limitation of the current public SDK surface, not (necessarily) an app bug. Cannot yet distinguish card-validation failure from a WebAuthn/passkey-stage failure from application-level evidence alone. | N/A (live observation, no code changed) |
| E017 | 2026-08-01T19:00:00Z | Remediation | Verify all four collectPAN failure paths are captured, and add stage/diagnostic tracking without any live call | Reviewed `src/app/gate-a2/page.tsx` against `CollectPANOptions` (`onError`, `onDismiss`, Promise rejection, synchronous SDK construction) | All four paths captured and recorded distinctly with sanitized diagnostics; no live call made during this fix | Found and fixed 3 real bugs: (1) `appendAuditEvent()` race condition (see E016) — now serializes writes through an internal promise queue (`src/lib/audit.ts`), proven by a new concurrent-write test; (2) `onDismiss`'s `payload.reason` was being silently discarded (callback ignored its parameter) — now routed through a new `GateA2Controller.dismiss(reason)` method that categorizes it via `categorizeMessage()` without ever logging the raw reason text; (3) `new PravaSDK(...)` construction was not wrapped in try/catch — a synchronous constructor throw (e.g. `INVALID_CONFIG`) would have been uncaught; now wrapped and routed through `controller.fail(err, "sdkInit")`. Added `src/lib/gateA2/diagnostics.ts` (`extractPravaErrorCode` — never invents a code, returns sentinel `UNAVAILABLE` when the SDK provides none; `categorizeMessage` — buckets human-readable text into safe categories, e.g. `SECURITY_CHECK_FAILED`, `AUTHENTICATION_FAILED`, without ever echoing raw text; `extractResponseIdSuffix` — truncates any response/request id found in `PravaError.details`). Extended `GateA2Controller`/`GateA2PublicState` with `stage` (`SDK_INITIALIZATION → IFRAME_LOADING → CARD_VALIDATION → SECURITY_CHECK → COMPLETION`, milestone-tracked via `markIframeReady()`/`markCardValidationComplete()`), `pravaErrorCode`, `sanitizedMessageCategory`, `onErrorObserved`, `onDismissObserved`, `promiseRejected`, `responseIdSuffix`. **Known limitation, not fixed**: the public `CollectPANOptions` surface (confirmed via the installed package's own `.d.ts`) does not expose a distinct WebAuthn-requested/rejected callback boundary — only `onReady`/`onChange`/`onSuccess`/`onError`/`onDismiss` are available, so `WEBAUTHN_REQUESTED`/`WEBAUTHN_REJECTED` stages are defined in the type but currently unreachable from this integration; both are bucketed under `SECURITY_CHECK` after card validation completes, which is honestly disclosed rather than guessed at. `passkeyPromptObserved` is likewise always `false` in the current integration for the same reason — never set to `true` without genuine evidence. | PASS | `src/lib/audit.ts` (+1 test), `src/lib/gateA2/diagnostics.ts` (+9 tests), `src/lib/gateA2/sessionManager.ts` (+7 tests), `src/app/gate-a2/page.tsx`, `src/lib/gateA2/eventSchema.ts` (+route test), 78/78 vitest tests pass, lint clean, typecheck clean, build succeeded | None — no live Prava call made during this remediation. Does not resolve E016's evidence gap for the *already-occurred* failed attempts (that data was never captured due to the bugs above and cannot be reconstructed after the fact); only prevents recurrence for future attempts. | 254e33c |
| E018 | 2026-08-01T19:15:00Z | B0/B1 | Deterministic rules engine produces the exact locked-scenario decisions and is a pure function | `npx vitest run src/lib/risk/rules.test.ts` | `routine-groceries` → APPROVE; `urgent-gift-cards` → REQUEST_TRUSTED_CONTACT; same input → same output | Confirmed both scenarios match exactly (score 0 → APPROVE; score 145 across 6 stacked risk signals → REQUEST_TRUSTED_CONTACT, `MULTIPLE_RISK_SIGNALS` correctly appended); purity verified via double-invocation equality check; sanitized intent schema confirmed to reject/strip card-shaped fields | PASS | `src/lib/risk/rules.test.ts` (6 tests) | none | 855ac6f |
| E019 | 2026-08-01T19:16:00Z | B2 | OpenAI structured-output layer never overrides the deterministic decision; fails closed, not open | Mocked `openai` package in `src/lib/risk/openaiExplanation.test.ts` — tested missing-key fallback, matching-decision success, mismatched-decision discard, invalid-schema fallback, thrown-error fallback, `store:false`/no-tools assertion, and privacy-boundary assertion on the request payload | REQUEST_TRUSTED_CONTACT never flips to APPROVE on any OpenAI failure mode; `store:false` always set; no tools; no userStatement/PII sent | All 7 tests pass. Confirmed via the installed SDK's own `.d.ts` (`node_modules/openai/resources/responses/responses.d.ts`, `node_modules/openai/helpers/zod.d.ts`) that `client.responses.parse({model, store, input, text:{format: zodTextFormat(...)}})` is the current, non-deprecated Structured Outputs API for the Responses endpoint — no deprecated Chat Completions syntax used, no invented SDK methods | PASS | `src/lib/risk/openaiExplanation.test.ts` (7 tests) | none | e9fb7c7 |
| E020 | 2026-08-01T19:17:00Z | B2/B3 | Privacy boundary: OpenAI receives only sanitized categorical features, never userStatement, card/identity, or Prava credentials | `moduleBoundary.test.ts` static source scan + `openaiExplanation.test.ts` payload-serialization assertion + `route.test.ts` audit-log assertion | No forbidden strings (`createPravaSession`, `PravaSDK`, `collectPAN`, `/api/prava/session`) appear in any risk-path source file; no PII/credential fields appear in the OpenAI request payload or in audit-log entries | Confirmed clean on all three fronts. `buildSanitizedPayload()` in `openaiExplanation.ts` explicitly omits `userStatement` and `scenarioId`'s free text — only categorical/numeric fields plus the deterministic decision/reason codes are sent | PASS | `src/lib/risk/moduleBoundary.test.ts`, `src/lib/risk/openaiExplanation.test.ts`, `src/app/api/risk/analyze/route.test.ts` | none | c2a53cd |
| E021 | 2026-08-01T19:23:37Z | B4 (live) | Live call 1/2 — routine-groceries scenario via `POST /api/risk/analyze` | Existing dev server (port 3600, pre-existing from Gate A2 session) hit once with the routine-groceries intent | HTTP 200, `decision=APPROVE`, ideally `explanationSource=OPENAI` | HTTP 200. `decision=APPROVE` (correct). **`explanationSource=DETERMINISTIC_FALLBACK`, `modelUsed=null`, latency `<500ms`, `schemaValid=false`** — `getOpenAIEnv()` threw before any network call was attempted, meaning `OPENAI_MODEL` and/or `OPENAI_API_KEY` are not both fully present/valid in `.env.local` yet. No OpenAI request actually reached the API for this call. | BLOCKED (OpenAI env) / PASS (deterministic decision + fallback correctness) | `data/audit-log.json` events `risk.intent.received`, `risk.rules.completed`, `risk.openai.fallback`, `risk.decision.ready`, `risk.prava.not_created` (inspected, reset) | `OPENAI_MODEL` (and possibly `OPENAI_API_KEY`) needs to be confirmed/added in `.env.local` before a genuine live OpenAI call can be attempted. Per the no-retry rule, this was not retried within this session. | c2a53cd |
| E022 | 2026-08-01T19:24:32Z | B4 (live) | Live call 2/2 — urgent-gift-cards scenario via `POST /api/risk/analyze` | Same dev server, hit once with the urgent-gift-cards intent | HTTP 200, `decision=REQUEST_TRUSTED_CONTACT`, no Prava session, no credential | HTTP 200. `decision=REQUEST_TRUSTED_CONTACT` (correct, all 6 risk signals + `MULTIPLE_RISK_SIGNALS` triggered). Same OpenAI-env blocker as E021 — `explanationSource=DETERMINISTIC_FALLBACK`, `modelUsed=null`. `pravaSessionCreated=false` and `paymentCredentialCreated=false` confirmed in the `risk.prava.not_created` event. | BLOCKED (OpenAI env) / PASS (deterministic decision + zero-Prava-session proof) | `data/audit-log.json` (inspected, reset) | Same as E021 — needs `OPENAI_MODEL`/`OPENAI_API_KEY` confirmed in `.env.local`. Two authorized live-call attempts have now been used; per instructions, no further live calls without new authorization. | c2a53cd |
| E023 | 2026-08-01T19:30:00Z | B0-B4 | Full automated verification suite passes before and after the live gate | `npx vitest run`, `npm run lint`, `npx tsc --noEmit`, `npm run build` | All pass cleanly, including pre-existing A0/A1/A2 suite | 107/107 vitest tests pass (29 new: rules, openaiExplanation, moduleBoundary, envBoundary, route, demo page source-safety), lint clean, typecheck clean, build succeeded — 8 routes generated including `/demo` and `/api/risk/analyze` | PASS | terminal output | none | c2a53cd |
| E024 | 2026-08-01T19:43:07Z | B4 (live, genuine) | Genuine live call 1/2 — routine-groceries, after `OPENAI_MODEL=gpt-5-mini` added to `.env.local` and a fresh dev server restart (port 3800) | `POST /api/risk/analyze` on the fresh server, routine-groceries intent | HTTP 200, `decision=APPROVE`, ideally `explanationSource=OPENAI`, `modelUsed=gpt-5-mini` | HTTP 200. `decision=APPROVE` (correct, deterministic). **This time the env loaded correctly — `modelUsed=gpt-5-mini` confirms `getOpenAIEnv()` succeeded and a real OpenAI request was attempted** (unlike E021, where it never left the process). However the call still fell back: `explanationSource=DETERMINISTIC_FALLBACK`, `schemaValid=false`, `responseIdSuffix=null`, `latencyBucket=>=4000ms` (measured ~8.02s, matching the 8000ms client-side `AbortController` timeout almost exactly) — the request timed out before a response was received, not a schema mismatch. Deterministic decision correctly preserved regardless. | BLOCKED (timeout) / PASS (deterministic decision + fail-safe fallback correctness) | `data/audit-log.json` events `risk.intent.received`, `risk.rules.completed`, `risk.openai.fallback`, `risk.decision.ready`, `risk.prava.not_created` (inspected, reset) | The ~8s timeout in `src/lib/risk/openaiExplanation.ts` (`TIMEOUT_MS = 8000`) is likely too short for this model's genuine round-trip latency, rather than an auth or model-name error (an auth/config error would fail near-instantly, not at exactly ~8s). Not retried per the no-retry rule; this authorized attempt is now used. | f230d40 |
| E025 | 2026-08-01T19:43:34Z | B4 (live, genuine) | Genuine live call 2/2 — urgent-gift-cards, same fresh server | `POST /api/risk/analyze`, urgent-gift-cards intent | HTTP 200, `decision=REQUEST_TRUSTED_CONTACT`, no Prava session/credential | HTTP 200. `decision=REQUEST_TRUSTED_CONTACT` (correct, all 6 signals + `MULTIPLE_RISK_SIGNALS`). Same timeout pattern as E024: `modelUsed=gpt-5-mini` (env loaded correctly, real attempt made), `explanationSource=DETERMINISTIC_FALLBACK`, `schemaValid=false`, `responseIdSuffix=null`, `latencyBucket=>=4000ms` (~8.02s). `risk.prava.not_created` confirms `pravaSessionCreated=false`, `paymentCredentialCreated=false`. Deterministic decision never flipped — the fail-closed fallback behavior worked exactly as designed under a second real failure. | BLOCKED (timeout) / PASS (deterministic decision + zero-Prava-session proof + fail-closed correctness) | `data/audit-log.json` (inspected, reset) | Same root cause as E024. Both of this round's two authorized genuine live-call attempts are now used; per instructions, no further OpenAI calls without new authorization. Recommend raising `TIMEOUT_MS` in `src/lib/risk/openaiExplanation.ts` (e.g. to 15000ms) before the next authorized attempt, as a candidate fix — not applied in this session since it wasn't requested and doing so wouldn't have been verifiable without a third live call. | f230d40 |
| E026 | 2026-08-01T20:00:00Z | Remediation | Replace hard-coded 8s local timeout with a configurable, bounded `OPENAI_TIMEOUT_MS` (default 30000ms) and disable SDK auto-retries | Reviewed installed SDK's own `.d.ts` (`node_modules/openai/client.d.ts`, `node_modules/openai/internal/request-options.d.ts`) confirming `timeout` and `maxRetries` are official per-request options on `client.responses.parse(body, options)`; added `parseOpenAITimeoutMs()` to `src/lib/env.ts` and removed the manual `AbortController`/`setTimeout` from `src/lib/risk/openaiExplanation.ts` in favor of the SDK's own `{ timeout, maxRetries: 0 }` request option — a single authoritative deadline, not stacked timeouts | Default 30000ms; valid override accepted; invalid/out-of-range values (non-numeric, zero, negative, non-integer, <5000, >60000) safely fall back to 30000 rather than throwing; `maxRetries: 0` always passed; never exposed via `NEXT_PUBLIC_*` | Confirmed via 6 new `env.test.ts` cases and 1 new `openaiExplanation.test.ts` assertion (`callOptions.maxRetries === 0`, `callOptions.timeout === 45000` for an explicit override). Also expanded sanitized failure classification (`src/lib/risk/openaiExplanation.ts`) to distinguish `LOCAL_TIMEOUT`, `OPENAI_API_TIMEOUT`, `AUTHENTICATION_ERROR`, `RATE_LIMIT`, `MODEL_ACCESS_ERROR`, `NETWORK_ERROR`, `STRUCTURED_OUTPUT_INVALID`, `DECISION_MISMATCH`, `UNKNOWN` — mapped via `instanceof` checks against the SDK's own exported error classes (`APIUserAbortError`, `APIConnectionTimeoutError`, `AuthenticationError`, `RateLimitError`, `NotFoundError`/`PermissionDeniedError`, `APIConnectionError`), confirmed against `node_modules/openai/core/error.d.ts`. 8 new tests cover every category, none of which ever forward raw error text (verified by explicit "must not contain" assertions using error messages that deliberately embedded sensitive-looking strings). | PASS | `src/lib/env.ts`, `src/lib/env.test.ts` (+8 tests), `src/lib/risk/openaiExplanation.ts`, `src/lib/risk/openaiExplanation.test.ts` (rewritten, 13 tests), `.env.example` | none | 1368ab9 |
| E027 | 2026-08-01T20:03:24Z | B4 (live, final) | The single authorized live OpenAI call — routine-groceries, with the 30s timeout and `maxRetries:0` in effect, after full dev-server restart (port 3900) | `POST /api/risk/analyze` exactly once, routine-groceries intent only | HTTP 200, `decision=APPROVE`, `explanationSource=OPENAI`, `schemaValid=true`, `modelUsed=gpt-5-mini` | **Success.** HTTP 200 in ~11.99s (well within the 30s budget). `decision=APPROVE` (correct, deterministic). `explanationSource=OPENAI`, `schemaValid=true`, `modelUsed=gpt-5-mini`, `responseIdSuffix=...37f6dd`, `failureCategory=null`. This confirms the Responses API + Structured Outputs + Zod validation path genuinely works end-to-end once the local timeout was long enough — the prior two "failures" (E024/E025) were indeed `LOCAL_TIMEOUT`-class, not auth/model/schema problems. `risk.prava.not_created` confirms `pravaSessionCreated=false`, `paymentCredentialCreated=false` even though `safeToCreatePravaSession=true` for this APPROVE result — no Prava session was created in this task, only eligibility recorded. | PASS | `data/audit-log.json` events `risk.intent.received`, `risk.rules.completed`, `risk.openai.completed`, `risk.decision.ready`, `risk.prava.not_created` (inspected, reset) | None. This was the single authorized call for this task; the `urgent-gift-cards` scenario was deliberately not called, per instructions. | 1368ab9 |
| E028 | 2026-08-01T20:32:16Z | B4 (live, final) | The single authorized live OpenAI call — urgent-gift-cards, same hardened 30s timeout + `maxRetries:0`, fresh dev-server restart (port 4000) | `POST /api/risk/analyze` exactly once, urgent-gift-cards intent only | HTTP 200, `decision=REQUEST_TRUSTED_CONTACT`, `safeToCreatePravaSession=false`, `credentialCreationAllowed=false`, `explanationSource=OPENAI`, `schemaValid=true`, `modelUsed=gpt-5-mini` | **Success.** HTTP 200 in ~12.37s (within the 30s budget). `decision=REQUEST_TRUSTED_CONTACT` (correct, deterministic — all 6 risk signals + `MULTIPLE_RISK_SIGNALS` triggered, unchanged from E025/E022). `explanationSource=OPENAI`, `schemaValid=true`, `modelUsed=gpt-5-mini`, `responseIdSuffix=...b6b2f0`, `failureCategory=null`, `decisionAcknowledged=REQUEST_TRUSTED_CONTACT` (matched, no mismatch/discard needed). `risk.prava.not_created` confirms `pravaSessionCreated=false`, `paymentCredentialCreated=false`. Both locked demo scenarios have now been genuinely validated end-to-end against the live OpenAI Responses API with Structured Outputs. | PASS | `data/audit-log.json` events `risk.intent.received`, `risk.rules.completed`, `risk.openai.completed`, `risk.decision.ready`, `risk.prava.not_created` (inspected, reset) | None. This was the single authorized call for this task; routine-groceries was deliberately not called again, per instructions. No Prava endpoint was called, `PravaSDK` was not initialized, `collectPAN` was not invoked (structurally impossible from this route — confirmed by the existing `moduleBoundary.test.ts` static scan, unchanged). | (pending) |
